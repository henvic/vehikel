<? // logout/index.phtml
$registry = Zend_Registry::getInstance();
$config = $registry->get("config");
$this->headTitle("Sign out");
?>
<div class="yui-gc">
<div class="yui-u first">
<h2>Sign out of <?= $this->escape($config->applicationname) ?></h2>
<p>If you ever forget to logout from a public computer, use remote logout.</p>
<?php if(isset($_GET['please']) || $registry->isRegistered("pleaseSignout")):?>
<p><strong>Notice:</strong> The action you tried to perform or the section you tried to access can not be performed by you while signed in to this account. Please, sign out and try again.</p>
<?php endif; ?>
<?php
if($this->remote_logout_done):
?>
<p><strong>Your other sessions have been signed out!</strong></p>
<?php endif; ?>
<?= $this->logoutForm ?>
</div>
</div>