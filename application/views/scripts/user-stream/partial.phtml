<?php

$paginator =  $this->posts;
/** @var $paginator \Zend_Paginator() */

if ($this->postsViewStyleIsTable) {
    echo $this->postTableView($paginator->getCurrentItems(), $this->userInfo);
} else {
    echo $this->postThumbnailView($paginator->getCurrentItems(), $this->userInfo);
}
?>