<?php
$registry = Zend_Registry::getInstance();
$config = $registry->get("config");

$order = $this->order;

$this->headTitle("Make a new call");
$signedUserInfo = $registry->get("signedUserInfo");
?>
<h2>Make a new call</h2>
<p>Choose which message file you want to use, the group you want to reach and the time the call should be delivered.</p>
<?php echo $this->callForm; ?>